name = "openrouter-apikey-manager"
main = "index.js"
compatibility_date = "2023-10-30"

# KV命名空间配置
# 注意：在部署时，如果没有指定ID，将会自动创建一个新的KV命名空间
kv_namespaces = [
  { binding = "API_KEYS", id = "temporary-id-for-deployment" }
]

# 路由配置（如果你有自定义域名）
# routes = [
#   { pattern = "your-domain.com/*", zone_name = "your-domain.com" }
# ]

# 触发器配置
[triggers]
crons = []
